export default {
    install(Vue, options) {
        const mixins = {
            data() {
                return {};
            },
            computed: {},
            methods: {}
        };
        Vue.mixin(mixins);
    }
};
